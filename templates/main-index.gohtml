{{ define "main-index" }}
    <!doctype html>
    <html>
    <head>
        <title>Tourtoster</title>
    </head>
    <body>
    <p>Hey!</p>
    <p>{{ .User.ID }}</p>
    <p>{{ .User.FirstName }} {{ .User.LastName }}</p>
    <p>{{ .User.Hotel.ID }}</p>
    <p>{{ .User.Hotel.Name }}</p>
    </body>
    </html>
{{ end }}